class Solution {
    public String reverseString(char[] s) {
        int n = s.length-1;
        for(int i=0;i<n/2+1;i++)
        {
            char temp=s[i];
            s[i]=s[n-i];
            s[n-i]=temp;
        }
        return Arrays.toString(s);
        
        
    }
    
}
